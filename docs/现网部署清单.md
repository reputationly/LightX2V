# 现网部署清单(2026-07-11 出包版)

> 硬件基线:每节点 4×A100 PCIE 40G / ARM 鲲鹏920 / 256G RAM(0010-0015 六节点)
> 镜像:lightx2v:arm64-a100-<出包日期>(含 P1-P9 补丁)

## 一、上线能力总表

| # | 能力 | model_cls | 权重(NFS) | 配置文件 | 形态 | 耗时 | 本次优化 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 1 | 文生视频 T2V | wan2.2_moe_distill | wan22_t2v_int8_seko(高/低噪) | `configs/deploy/wan22_t2v_int8_4card_a100.json` | 4卡 ulysses·int8·4步·720p·RIFE 32fps | **133s**/5s条 | **torchao→triton(+6%)**;boundary_step_index=2 固化进配置 | ✅现网升级 |
| 2 | 图生视频 I2V | wan2.2_moe_distill | Wan2.2-I2V-720p-int8(高/低噪) | `configs/deploy/wan22_i2v_int8_4card_a100.json` | 4卡 ulysses·int8·4步·720p·RIFE 32fps | **68s**/5s条 | **torchao→triton(+21%)** | ✅现网升级 |
| 3 | 数字人-标准档 480p | infinitetalk | Wan2.1-Distill 480p int8 4step 单文件+wav2vec | 480p 4卡 ulysses 配置(int8-triton) | **4卡 ulysses×1实例/节点**(用户拍板 2026-07-11:双档全4卡, 时延优先) | **54.4s**/5s条(~1.1条/分/节点) | P7 补丁必需;单DiT可用 load_from_rank0 提速加载;时长红线 60-75s | 🆕新上线 |
| 3b | 数字人-高清档 720p | infinitetalk | 同上(720p 蒸馏 DiT) | `configs/infinitetalk/a100/infinitetalk_720p_long_int8triton.json` | 4卡 ulysses×1实例/节点 | **136.7s**/5s条 | 时长红线更紧(帧体积3×, 网关按档限长, 建议≤25-30s音频起步实测再放) | 🆕新上线 |
| 4 | **视频超分(原子能力+画布)** | seedvr2 | SeedVR2-3B | `configs/seedvr/a100/seedvr2_3b_seg121.json` | 单卡·bf16·≤121帧单段无缝(超长自动分段) | 480p→2×约80s / 720p→2×约200s | 改判 2026-07-11:**升级为原子能力**(原"只进画布"拍板作废);P6 补丁(含分段 pts 修复);sr_ratio 锁 2.0 | 🆕新上线 |
| 5 | 视频编辑 VACE-**原子档** | wan2.2_moe_vace | Wan2.2-VACE-Fun-int8(高/低噪)+**Lightning LoRA**(x2v格式) | `configs/wan22/a100/wan22_moe_vace_a100_int8_lora4step_720p_rife.json` | **单卡 offload×2实例/节点**(密度上限=host内存, 每实例pin~55G)·int8-triton·**4步LoRA**·**720p 原生**·RIFE 32fps | **单实例418s/双实例485s(膨胀16%, 压测实测)**;常驻免加载更短 | SR只许进画布(拍板)→原子档裸出720p;LoRA 13×(P8/P9);剩余2卡混部InfiniteTalk等host便宜能力 | ⏸随 schema 改造上线* |
| 5b | 视频编辑 VACE-画布档(画布上线时启用) | wan2.2_moe_vace | 同上 | `configs/wan22/a100/wan22_moe_vace_a100_int8_lora4step.json` | 单卡·480p→SeedVR2 1664×960(链路末端统一插帧) | 82s+SR 80s≈**2.7min** | 比原子档更快更清晰;SR/插帧编排遵循画布规则 | ⏸随画布 |
| 6 | 文生图 | z_image | Z-Image-Turbo | (见 Z-Image 报告) | **单卡 bf16×4实例/节点** | 7.6s/张(0.53张/s/节点) | int8 判死勿用;须热态预热 | ✅已验待排期 |
| 7 | 图像生成/编辑 Qwen | qwen_image | Qwen-Image+Lightning merged | `configs/deploy/qwen_2512_lightning_merged_a100.json` / `qwen_edit_2511_...` | 单卡 | — | 存量能力, 随镜像滚动 | ✅现网存量 |

*VACE 上线前置:server schema 补 `src_video/src_ref_images/src_mask` 字段 + 落盘逻辑;画布侧配预处理节点(mask灰填/canny)。

## 一.5、节点布局(6节点示例, 按流量调)

```
├─ 节点A: T2V 4卡 ulysses ×1                      (133s/条)
├─ 节点B: I2V 4卡 ulysses ×1                      (68s/条)
├─ 节点C: InfiniteTalk-480p 4卡 ulysses ×1        (54.4s/条)
├─ 节点D: InfiniteTalk-720p 4卡 ulysses ×1        (136.7s/条)
├─ 节点E: VACE ×2(单卡offload, host~55G/实例) + SeedVR2 ×2(单卡, host~42G/实例)
└─ 节点F: Z-Image ×4 / Qwen / 机动扩容
```

- InfiniteTalk 双档 = 不同配置的独立实例, 网关按请求分辨率路由到对应节点
- 节点E host 合计 ~194G, 是唯一贴水位的节点, 上线后盯 MemAvailable
- 扩容优先级按排队情况: I2V(68s最热门)> InfiniteTalk > 其他

**SeedVR2 原子能力注意事项**(改判后新增):
1. **sr_ratio 锁死 2.0**(代码级):480p入→1664×960 出、720p入→2560×1440 出——输出分辨率跟着输入走, 产品文案按"2倍超分"表述;要"任意→1080p"需加 schema 字段(二期)
2. **输入上限**:分辨率 ≤720p(1080p入→4K出显存未验证, 先在 schema/网关侧拦);帧数 ≤121 单段无缝, 超过自动分段(161帧实测质量OK, 且本次已修 PyAV 分段 pts bug)
3. **fps 透传不插帧**:SR 保持输入帧率;原子SR的输入通常是用户已有视频(24/30fps), 不强加 RIFE;16fps 生成片要丝滑请走"生成能力自带的 RIFE"
4. **资源**:显存峰值 ~22.7G、host ~42G, 都便宜——**与 VACE 混部完美**:节点布局 2×VACE + 2×SeedVR2, 同时服务原子SR请求和(画布期的)VACE后处理链
5. video_path 输入链路已在测试中全程验证(harness 走的就是 server 任务体)

## 二、明确不上/已判死

| 对象 | 判决 |
|---|---|
| Wan2.2-S2V | 降级为基线(bf16+offload 22min);生产由 InfiniteTalk 替代 |
| LTX2.3 | 40G 多卡全 OOM、int8 无收益画质崩 |
| VACE 720p **4卡**常驻 | **死刑三审终审**:P10v1(mask数学挪CPU)、P10v2(+像素张量驻CPU+use_tiling_vae 分块编解码)两轮补丁把墙从 VAE 一路拆到 DiT 本体——最终 OOM 在 pre_infer patchify(需774MB, 剩344MB)。物理结论:**38G权重常驻 + 720p VACE 序列的瞬时峰在 40G 卡上无解**, 再拆=重构DiT推理路径, 不立项。P10 已回滚不出包。附带正面发现:wan VAE 原生支持 use_tiling_vae 分块编解码+多卡分片编码(记档备用) |
| VACE **4实例/节点** | **死刑**(压测实锤):host内存最低61MB濒死+四实例同时冷启把NFS读打进病态(3×T5并发18min);密度上限2实例 |
| **VACE 创作档(40步4卡)** | 625s 太长撑不起产品体验(用户拍板 2026-07-11);大动态需求走生产 t2v/i2v;配置 ul4 留档不部署 |
| VACE extend 续写 | 不连贯+手变形;首帧动画归 i2v |
| wan2.1_vace | LoRA 解决速度后只剩画质降级, 不做 |
| bf16 多卡(所有 MoE 模型) | 256G 内存必 CPU OOM |
| load_from_rank0 + MoE | 不兼容必崩(仅单 DiT 模型可用) |
| seq_p 通信优化(fp8_comm/head_parallel) | 无 NVLink+ARM 栈无软件解, 勿再尝试 |

## 三、部署运维铁律(全能力通用)

1. **多卡禁 cpu_offload**(per-rank pin 爆 host 内存);MoE 禁 load_from_rank0
2. **triton 首请求 per-shape autotune 慢**——实例启动后预热一发再挂流量
3. **错峰启动=必须**(从"建议"升格):压测实锤——4实例同时冷启, 3×T5(11.4G .pth)并发读NFS 18分钟不动;生产实例必须间隔≥2分钟顺序拉起
4. 压测/测数须安静宿主;容器一律 `--memory=240g` 双保险
5. **VACE 三个后续优化杠杆**:①常驻实例(免每请求加载摊销)②SR 流水线化(画布期)③**NUMA 绑定**——双实例各绑一个 NUMA 域(`--cpuset-cpus/--cpuset-mems`+选不同 PCIe 根的两张卡, 先 `nvidia-smi topo -m` 查映射), 隔离 offload 搬运的带宽域, 预期压掉部分 16% 膨胀(InfiniteTalk 2卡+NUMA 实测 +4% 同方向)
6. **TP/PP 判死**:框架有 tensor_p 管线但 wan 系模型未实现(需全线性层切分+int8感知加载=深度工程);无 NVLink 下 TP 每层 2×all-reduce 通信惩罚重(ulysses 只在 attention 边界通信, 正是为 PCIe 选的);PP 无实现+单样本气泡大
6. 观察名单:SekoTalk 权重开放即测(runner/config 现成, 25fps 实时+AR流式);Wan2.2-Animate(需官方预处理管线+decord补丁)
