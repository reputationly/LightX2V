# 出包清单 — 合库评审(2026-07-11 终版, VACE 全线收官后)

> 目标:把测试产出的补丁/配置/脚本**一把**合入自库,重建 `lightx2v:arm64-a100` 镜像(用户拍板:全部搞完统一出包)。
> 规矩:**逐文件评审确认后才 commit**;补丁不提上游(用户拍板 2026-07-10)。

## 一、源码补丁(9 个, 均已挂卷实测验证)

| # | 文件 | 改了什么 | 为什么 | 影响面 |
|---|---|---|---|---|
| P1 | `lightx2v/common/ops/tensor/tensor.py` | DefaultTensor 加 `__getattr__`:访问 `.tensor` 缺失且有 pin_tensor 时按需搬 GPU | cpu_offload 下裸 `.tensor` 访问必 AttributeError(S2V 触发) | **公共文件**;仅在"原本必崩"的路径触发,正常路径零变化 |
| P2 | `lightx2v/models/networks/wan/infer/s2v/wan_ops.py` | flash_attn3 缺失时回退 torch SDPA(varlen 打包前,按 k_lens 建 mask) | 上游硬编码 fa3(Hopper 专属),A100 无解 | 仅 S2V |
| P3 | `lightx2v/models/networks/wan/infer/s2v/transformer_infer.py` | 新增 WanS2VOffloadTransformerInfer(块权重走 manager 双缓冲,audio_inject 常驻 GPU) | S2V+cpu_offload 组合上游未实现 | 仅 S2V |
| P4 | `lightx2v/models/networks/wan/s2v_model.py` | `_init_infer_class` 按 cpu_offload 选推理类 | 原来无条件用非 offload 类(P3 问题的总根源) | 仅 S2V |
| P5 | `lightx2v/models/runners/wan/wan_s2v_runner.py` | VAE encode 前输入显式 `.to(AI_DEVICE)` | offload 下输入留 CPU/VAE 在 CUDA → 混合设备 conv3d 崩 | 仅 S2V |
| P6 | `lightx2v/models/runners/seedvr/seedvr_runner.py` | `_probe_video` 的 torchvision `read_video_timestamps` 加 PyAV 回退 | torchvision ≥0.23 移除该 API,新镜像必崩 | 仅 SeedVR2 |
| P7 | `lightx2v/models/networks/wan/infer/infinitetalk/transformer_infer.py` | audio cross-attn 的 proj 前按需拍平 3D 输出 | torch_sdpa 与 sage/flash 输出形状契约不一致(4D 输入时) | 仅 InfiniteTalk;**生产部署必需** |
| P8 | `lightx2v/models/input_encoders/hf/vace/vace_processor.py` | decord 不可用时回退 `_PyAVVideoReader`(接口对齐 decord 所需子集) | ARM 镜像 decord 是空壳包(无 bridge), VACE V2V/MV2V 必崩;PyAV 解码像素级一致零代价 | 仅 VACE 视频输入路径;x86 decord 正常时走原路径 |
| P9 | `lightx2v/models/networks/wan/vace_model.py` | `WanVaceModel.__init__` 签名补 `lora_path/lora_strength` 透传 | 上游动态 LoRA 只实现到标准 Wan 类, VACE 子类漏透传 → `lora_dynamic_apply` 秒崩;**4步蒸馏LoRA(13×提速)的钥匙** | 仅 VACE;一行签名修复 |
| P10 | `lightx2v/server/api/tasks/{common,video}.py` | `[latency-probe]` 埋点×4:提交(带排队数)/状态轮询/**轮询MISS告警**(亲和性错配证据)/取片速度(MB/s) | 现网感知时延>实际生成时延, 出包后拿日志定位轮询间隔/亲和错配/大文件代理哪个背锅, 再决定是否上回调 | server 全局;**纯日志零行为变更**, 14行 |

## 二、测试工具(4 个脚本 + harness 增强)

| 文件 | 说明 |
|---|---|
| `scripts/smoke/test_model.sh`(改) | 加 VIDEO_PATH/VIDEO_DURATION/PORT/EXTRA_VOL 支持 |
| `scripts/smoke/run_batch.sh` / `run_batch2.sh`(新) | 任务编排(防粘贴断行/防端口撞/串行队列);W1 A/B、真实感四实验 |
| `scripts/smoke/test_infinitetalk_stress.sh`(新) | 压测:单/多卡、CPU/显存/host内存 2s 采样 CSV、稳态统计、NP>1 自动 load_from_rank0 |
| `scripts/smoke/vace_*.sh` + `vace_prep_*.py`(新, 10个) | VACE 全家桶:int8首跑/ul4/v2v/canny/rife/modes(inpaint灰填/outpaint/extend)/lora A|B/720p/SR终验 + 输入构造(mask/灰填/canny) |
| `scripts/smoke/tbest.sh`(新) | 真实感天花板链(摄影级提示词→t2v 720p→SeedVR2) |
| `scripts/download_infinitetalk.sh` / `download_vace_model.sh` / `download_vace_lora.sh`(新) | ModelScope 优先+续传+完整性自检;LoRA 含 x2v 格式转换步骤 |

## 三、配置(4 组 a100 目录, 全部实测)

- `configs/infinitetalk/a100/`:**生产主配置** `infinitetalk_480p_single_distilled_int8triton.json`(93.8s 冠军)+ 720p/多人/长音频/变体
- `configs/seedvr/a100/`:3B/7B/7B锐化 + **单段无缝版** `seedvr2_3b_seg121.json`(生产建议)
- `configs/wan22/a100/`:S2V 系列 + W1 系列 A/B + **VACE 全家**(int8 / ul4 / rife / **lora4step=生产编辑档** / lora4step_720p_1card / lora4step_ul4_720p=已判死留档)
- **✅ 已定稿:`configs/deploy/wan22_{i2v,t2v}_int8_4card_a100.json` 切 `int8-triton`**(W1 三组同seed画质终审"看不出差异", i2v 提速21%/t2v 6%);t2v 并固化 `boundary_step_index: 2`(原依赖 launcher 注入)

## 四、文档(6 份)

- 实验报告 ×5:SeedVR2 / Wan2.2-S2V / InfiniteTalk / **Wan2.2-真实感调查**(新) / **Wan2.2-VACE**(新, 16实验点+LoRA移植)
- 优化总结与Wan2.2移植清单(W1/W2/W3/W5 全部结案)+ 交接文档增补 + 本评审清单

## 五、出包步骤(评审通过后)

1. 逐文件 git add + commit(分 3 个 commit:①源码补丁 P1-P9 ②工具+配置(含 deploy 切 triton) ③文档)
2. 重建镜像 `lightx2v:arm64-a100-<date>` → retag latest → 238 跑 `prepare-transfer` 更新 NFS tar
3. 灰度:一台节点新镜像部署 InfiniteTalk int8-triton 实例,压测回归(稳态应=93.8s);另起 wan2.2 t2v/i2v triton 实例各出一片对样
4. 全量:其余节点滚动升级;deploy 配置的 triton 切换随镜像同车发布
5. 后续立项(不阻塞出包):VACE 编辑能力上服务(schema 加 src_video/src_ref_images/src_mask + 预处理节点)/ seko_talk 数字人二代调研
